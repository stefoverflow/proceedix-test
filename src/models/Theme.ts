type Theme = {
  colors: {
    lightTextColor: string;
    textColor: string;
    lightGrey: string;
    darkGrey: string;
    white: string;
    mainBlue: string;
  };
  boxShadow: string;
  borderRadius: string;
  fontSize: string;
  titleFontSize: string;
};

export default Theme;
