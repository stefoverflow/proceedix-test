type Theme = {
  colors: {
    lightTextColor: string;
    textColor: string;
    purple: string;
    lightGrey: string;
    darkGrey: string;
    white: string;
    mainBlue: string;
  };
  boxShadow: string;
  borderRadius: string;
  fontSize: string;
  titleFontSize: string;
};

export default Theme;
